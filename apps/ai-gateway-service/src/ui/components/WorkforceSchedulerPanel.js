export function renderWorkforceSchedulerPanel() {
  return `
                <section class="workforce-preview-card" id="workforce-scheduler-panel">
                  <strong>本地任务队列策略</strong>
                  <p>生成计划 -> 保存计划 -> 建立任务队列 -> 本地完成记录 -> 写入 Phase1961A evidence。</p>
                  <p>maxCandidateEmployees=5 / maxActiveEmployees=3 / maxBrainCalls=0 / requireEvidence=true</p>
                  <small>不广播全模型目录 / 不触发Provider 风暴 / 不读取密钥/small>
                </section>`;
}


