export const providerCredentialCopy = {
  cardCopy: "这里管理 Provider 配置状态。页面不会回显 API Key 明文，也不会把未验证配置说成已连接。",
  guardedNotice: "Provider 只通过 credentialRef 引用；普通界面不显示 secret，不绕过 selectable gate。",
  guidance: "当前阶段只做本地 UI 收口。保存配置、连接测试和真实调用仍受既有后端 gate 控制，不在本阶段扩大 Provider 请求。"
};
