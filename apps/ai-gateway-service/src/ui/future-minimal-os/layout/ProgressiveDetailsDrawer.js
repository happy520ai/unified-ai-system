import { renderProgressiveDisclosurePanel } from "../ProgressiveDisclosurePanel.js";

export function renderProgressiveDetailsDrawer({ copy, detailModules, diagnosticsModules }) {
  return renderProgressiveDisclosurePanel({ copy, detailModules, diagnosticsModules });
}
