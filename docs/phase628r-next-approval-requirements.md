# Phase628R Next Approval Requirements

Before any future main-chain integration phase:
- separate explicit approval required
- separate `/chat` approval required
- separate `/chat-gateway/execute` approval required
- separate provider runtime modification approval required
- separate production rollout approval required
- separate release approval required
