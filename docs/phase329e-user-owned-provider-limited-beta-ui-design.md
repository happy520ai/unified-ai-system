# Phase329E User-Owned Provider Limited Beta UI Design

The UI workflow is beta-only and does not activate production provider runtime automatically.

It accepts credential references only and rejects secret-like direct values.

The UI never calls providers directly; backend governance remains mandatory.
