# Phase596 Task 9 Mission Control Usage Note

Mission Control displays the Codex Context Gateway operator panel as a secondary operator/debug area. Phase596 adds a benchmark preview that summarizes executed tasks, average token saving, relevant file usage, full repo scan avoidance, stale guard simulation, validation status, and next optimization.

This UI preview reads generated benchmark evidence and local `.codex-context` outputs. It is not a live Codex connection and does not send prompts to a Provider.

The panel must keep all buttons responsive and must not hide or replace the existing sample dry-run and Workforce preview entry points.
