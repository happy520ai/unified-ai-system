# Phase609R Local Demo Quickstart

```powershell
cmd /c pnpm install
cmd /c pnpm verify:phase609r-public-dry-run-demo-package
cmd /c pnpm smoke:phase308a-desktop-workbench-ui
```

Open Mission Control through the local Workbench UI after running the local service in an approved dry-run path. This quickstart does not need an API key and does not call real Providers.

- localDemoQuickstartGenerated=true
- demoValidationPassed=true
