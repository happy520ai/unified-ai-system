# Phase901 Provider Call Authenticity Contract

Defines the evidence contract for distinguishing local guarded executor attempts, dry-run, mock, simulated, and actual external Provider API calls.

Boundary: no Provider call, no raw secret read, no auth.json read, no default `/chat` or `/chat-gateway/execute` mutation.
