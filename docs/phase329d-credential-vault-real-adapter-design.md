# Phase329D Credential Vault Real Adapter Design

Phase329D adds adapter routing for env key name, encrypted reference, and vault reference.

Only env key-name metadata is supported. Encrypted and vault references are explicit not-ready adapters unless real services are configured later.
