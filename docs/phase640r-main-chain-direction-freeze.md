# Phase640R Main-Chain Direction Freeze

## Freeze Decision

- Main-chain path frozen by user direction.
- Provider-runtime path frozen by user direction.
- Future reopen requires explicit user request.
- Existing Phase629/630 docs remain historical design references only.
- Do not continue main-chain phases automatically.

## Current Interpretation

Codex/crs remains an external tool mode for token-saving and engineering task
assistance. It is not a default `/chat` lane, not a `/chat-gateway/execute`
lane, not a Provider runtime mutation, and not a production traffic route.
