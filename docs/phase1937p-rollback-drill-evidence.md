# Phase1937P Rollback Drill Evidence

This phase runs a rollback drill in dry-run mode by default.

Forbidden:
- `git reset --hard`
- `git clean`
- Deleting real files
- Claiming dry-run rollback as real rollback

Real rollback remains blocked without explicit owner authorization.
