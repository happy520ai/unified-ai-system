# Phase329D Credential Access Policy

Credential access requires:

- no secret value in request
- user-owned credential scope
- allowed provider scope
- allowed mode scope
- audit trace enabled

Raw secret values are never returned.
